name: app_usage
description: Live glassy Android app usage counter with today's per-app totals.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.13.0-238.0.dev

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  get_it: ^8.0.3
  dartz: ^0.10.1
  equatable: ^2.0.7
  gap: ^3.0.1
  shared_preferences: ^2.5.3
  intl: any
  usage_stats: ^2.0.1
  flutter_overlay_window: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  generate: true
  uses-material-design: true
